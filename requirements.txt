# ============================================================
# 核心依赖 (Colab 和本地环境通用)
# ============================================================

# PyTorch (选择一个版本)
# 对于 GPU (NVIDIA CUDA 11.8+):
torch==2.1.0
# 对于 CPU only (取消下一行注释):
# torch==2.1.0 --index-url https://download.pytorch.org/whl/cpu

# 核心推理库
transformers==4.36.0
peft==0.7.0
numpy==1.24.3
tqdm==4.66.0

# 语义搜索和 RAG
sentence-transformers==2.2.2
safetensors==0.4.0

# ============================================================
# 训练依赖 (仅 Colab 训练时需要)
# ============================================================

datasets>=2.18.0
accelerate>=0.24.0
# bitsandbytes>=0.41.0  # 8-bit 量化 (可选)

# ============================================================
# 数据处理
# ============================================================

pyyaml>=6.0
sentencepiece>=0.2.0

# ============================================================
# API 服务 (仅使用 API 模式时需要)
# ============================================================

flask==3.0.0
flask-cors==4.0.0
gunicorn==21.0.0  # 生产环境 WSGI 服务器

# ============================================================
# 开发工具 (可选)
# ============================================================

jupyter>=1.0.0
notebook>=7.0.0
ipython>=8.16.0
black>=23.0.0
pylint>=3.0.0

# ============================================================
# 监控和日志 (可选)
# ============================================================

# tensorboard>=2.14.0
# wandb>=0.16.0
python-dotenv>=1.0.0

# ============================================================
# 云服务 (可选)
# ============================================================

# openai>=1.0.0           # OpenAI API
# dashscope>=1.14.0       # 阿里云通义千问
# google-cloud-storage    # Google Cloud Storage
# boto3                   # AWS S3

# ============================================================
# GIS 相关 (可选，如需要空间数据处理)
# ============================================================

# geopandas>=0.14.0
# rasterio>=1.3.6
# shapely>=2.0.1
# fiona>=1.9.4
# pyproj>=3.6.0
# scikit-learn>=1.2.0
